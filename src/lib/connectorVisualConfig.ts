/**
 * Connector design system: token underlines, StoryEditor.
 * Connectors page uses connectorsPageConfig.ts for grid sandbox; yin-yang preview colors below.
 */

/** Underline thickness in px. Used by tokenStyling, TokenDisplay. */
export const UNDERLINE_THICKNESS = 15

/** `/connectors` KoruConnectorDesigner: western lobe + left rod (smaller x). */
export const CONNECTOR_PREVIEW_FILL_WEST = '#166534'
/** `/connectors` KoruConnectorDesigner: eastern lobe + right rod. */
export const CONNECTOR_PREVIEW_FILL_EAST = '#1e40af'
